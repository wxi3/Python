spam = ['a,','b','c','d','e']

def backstr(spam):
    str = ''
    for i in range(len(spam) - 1):
        str = str + spam[i] + ','

    str = str + 'and ' + spam[i+1]
    

    return str





print(backstr(spam))
    
